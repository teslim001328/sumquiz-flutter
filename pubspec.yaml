name: myapp
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.5.0-18.0.dev <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # Firebase and Google Packages (updated to your verified versions)
  firebase_core: ^4.1.1
  firebase_auth: ^6.1.0
  cloud_firestore: ^6.0.3
  google_sign_in: ^7.2.0
  firebase_app_check: ^0.4.1
  firebase_ai: ^3.3.0

  # Other updates from your instructions
  fl_chart: ^1.1.1
  intl: ^0.20.2
  syncfusion_flutter_core: ^31.1.19
  syncfusion_flutter_pdf: ^31.1.19
  image_picker: ^1.0.4


  # Unchanged dependencies
  cupertino_icons: ^1.0.8
  provider: ^6.1.2
  file_picker: ^10.3.3
  in_app_purchase: ^3.2.0
  shared_preferences: ^2.2.3
  http: ^1.2.1
  url_launcher: ^6.3.0
  connectivity_plus: ^7.0.0
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  path_provider: ^2.1.3
  uuid: ^4.5.1
  flutter_card_swiper: ^7.0.1
  flip_card: ^0.7.0
  google_fonts: ^6.3.2
  share_plus: ^12.0.0
  rxdart: ^0.28.0
  go_router: ^16.2.5
  confetti: ^0.8.0
  flutter_quill: ^11.4.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  hive_generator: ^2.0.1
  mockito: ^5.4.0
  build_runner: ^2.4.0
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
