name: myapp
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.5.0-18.0.dev <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2

  # Firebase and Google Packages - Let the solver find compatible versions
  firebase_core: any
  firebase_auth: any
  cloud_firestore: any
  google_sign_in: any
  firebase_app_check: any
  firebase_ai: any

  # UI and Utility Packages
  provider: ^6.1.2
  file_picker: ^8.0.5
  syncfusion_flutter_pdf: ^25.2.7
  fl_chart: ^0.68.0 # Use a version compatible with the solver
  in_app_purchase: ^3.2.0
  in_app_purchase_storekit: ^0.4.0
  shared_preferences: ^2.2.3
  http: ^1.2.1
  flutter_markdown: ^0.7.3
  url_launcher: ^6.3.0
  connectivity_plus: ^6.0.3
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  path_provider: ^2.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  mockito: ^5.4.4
  build_runner: ^2.4.10
  hive_generator: ^2.0.1

flutter:
  uses-material-design: true
